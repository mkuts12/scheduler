A simple project using 'genetic' library.
This project will output the best schedule, based on a threshold, with respect to plugins you can write.

It is still a first draft but it works on simple case.
